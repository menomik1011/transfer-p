import styled from "styled-components";

const LoginBlock = styled.div`
    width: 350px;
    border: 1px solid #dbdbdb;
    background-color: #fff;
`

export default function Login(){
    return <LoginBlock>login</LoginBlock>
}